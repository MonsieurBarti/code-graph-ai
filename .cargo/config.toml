[target.aarch64-unknown-linux-gnu]
# Work around linker OOM (SIGKILL) in memory-constrained CI environments.
# --no-keep-memory reduces peak RSS during link at the cost of slower links.
rustflags = ["-C", "link-arg=-Wl,--no-keep-memory"]
